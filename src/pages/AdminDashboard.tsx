import {useEffect,useState} from 'react';
import {CheckCircle,Search,ShieldAlert} from 'lucide-react';
import {Seo} from '../components/Seo';import {supabase} from '../lib/supabase';import {maskLogin,statusLabel} from '../lib/managedStrategy';
type Row={user_id:string;member_email:string;broker_entity:string;mt5_server:string;masked_mt5_login:string;affiliate_status:string;strategy_status:string;risk_acknowledged:boolean;submitted_at:string;updated_at:string};
export function AdminDashboard(){const [rows,setRows]=useState<Row[]>([]),[search,setSearch]=useState(''),[message,setMessage]=useState('');const load=async()=>{const {data,error}=await supabase.rpc('admin_list_strategy_applications',{query:search});if(error)setMessage(error.message);else setRows(data||[])};useEffect(()=>{load()},[]);const update=async(row:Row,affiliate:string,strategy:string)=>{const reason=prompt('Reason for this status change (recorded in the immutable audit log):');if(reason===null)return;const {error}=await supabase.rpc('admin_set_strategy_status',{member:row.user_id,affiliate,strategy,detail:reason});if(error)setMessage(error.message);else{setMessage('Status updated and audited.');load()}};return <><Seo title="Managed Strategy Admin | Apex Trade Network" description="Protected strategy review console."/><section className="section admin-strategy"><header><p className="eyebrow">Protected administrator area</p><h1>Managed Strategy Reviews</h1><p><ShieldAlert size={16}/> Authorization is checked from signed app metadata and enforced by database RPCs.</p></header><form className="admin-filters" onSubmit={e=>{e.preventDefault();load()}}><label><span>Search member email, broker or server</span><input value={search} onChange={e=>setSearch(e.target.value)} maxLength={100}/></label><button className="cta"><Search size={15}/> Search</button></form><p role="status">{message}</p><div className="admin-records">{rows.map(row=><article key={row.user_id}><div className="admin-record-head"><div><h2>{row.member_email}</h2><p>{row.broker_entity} · {row.mt5_server}</p></div><span className="status-pill">{statusLabel(row.affiliate_status)}</span></div><dl><div><dt>MT5 Login</dt><dd>{row.masked_mt5_login||maskLogin('')}</dd></div><div><dt>Strategy</dt><dd>{statusLabel(row.strategy_status)}</dd></div><div><dt>Risk acknowledgement</dt><dd>{row.risk_acknowledged?'Accepted':'Missing'}</dd></div><div><dt>Submitted / updated</dt><dd>{new Date(row.submitted_at).toLocaleString()} · {new Date(row.updated_at).toLocaleString()}</dd></div></dl><div className="strategy-actions"><button className="cta" onClick={()=>update(row,'verified','mt5_account_ready')}><CheckCircle size={15}/> Verify affiliate</button><button className="cta cta-secondary" onClick={()=>update(row,'pending','affiliate_verification_pending')}>Request information</button><button className="danger-button" onClick={()=>update(row,'not_verified','affiliate_not_verified')}>Reject</button><button className="cta" onClick={()=>update(row,'verified','strategy_active')}>Activate strategy</button></div></article>)}</div></section></>}
